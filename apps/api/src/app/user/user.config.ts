/*********************************
 PATHS
 *********************************/
import { ApiPath } from '../../interfaces/api.model';

export const USER_PATHS = {
  name: 'user',
  user: {
    profile: { name: 'profile' }
  }
};

const USER_PATHS_check: ApiPath = USER_PATHS;
